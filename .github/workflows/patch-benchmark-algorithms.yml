name: Patch benchmark algorithms

on:
  push:
    paths:
      - '.github/workflows/patch-benchmark-algorithms.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch benchmark
        run: |
          python - <<'PY'
          from pathlib import